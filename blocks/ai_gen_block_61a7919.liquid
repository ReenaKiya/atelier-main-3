{% doc %}
  @prompt
    Create a custom section with a background image that includes:
    - A title positioned on the left side above the carousel
    - Collection navigation tabs on the right side that act as filters
    - A product carousel in the center-right area that dynamically changes based on which collection tab is selected
    - The carousel should display products from the selected collection
    - Smooth transitions when switching between collection tabs
    - Mobile-responsive layout where title and tabs stack vertically on smaller screens
    - Customizable background image, title text, and collection selections in the theme editor
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-collection-carousel-{{ ai_gen_id }} {
    display: block;
    position: relative;
    width: 100%;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    padding: {{ block.settings.section_padding_top }}px 0 {{ block.settings.section_padding_bottom }}px;

    min-height: 500px;
  }

  .ai-collection-carousel-overlay-{{ ai_gen_id }} {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: {{ block.settings.overlay_color }};
    opacity: {{ block.settings.overlay_opacity | divided_by: 100.0 }};
    pointer-events: none;
  }

  .ai-collection-carousel-container-{{ ai_gen_id }} {
    position: relative;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 40px;
  }

  .ai-collection-carousel-header-{{ ai_gen_id }} {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 40px;
    gap: 20px;
  }

  .ai-collection-carousel-title-{{ ai_gen_id }} {
    color: {{ block.settings.title_color }};
    font-size: {{ block.settings.title_size }}px;
    margin: 0;
    flex: 0 0 auto;
  }

  .ai-collection-carousel-tabs-{{ ai_gen_id }} {
    display: flex;
    gap: 24px;
    flex-wrap: wrap;
    justify-content: flex-end;
  }

  .ai-collection-carousel-tab-{{ ai_gen_id }} {
    background: none;
    border: none;
    color: {{ block.settings.tab_color }};
    font-size: {{ block.settings.tab_size }}px;
    cursor: pointer;
    padding: 8px 0;
    position: relative;
    transition: color 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .ai-collection-carousel-tab-{{ ai_gen_id }}::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background-color: {{ block.settings.tab_active_color }};
    transition: width 0.3s ease;
  }

  .ai-collection-carousel-tab-{{ ai_gen_id }}.active {
    color: {{ block.settings.tab_active_color }};
  }

  .ai-collection-carousel-tab-{{ ai_gen_id }}.active::after {
    width: 100%;
  }

  .ai-collection-carousel-tab-{{ ai_gen_id }}:hover {
    color: {{ block.settings.tab_active_color }};
  }

  .ai-collection-carousel-content-{{ ai_gen_id }} {
    position: relative;
  }

  .ai-collection-carousel-track-{{ ai_gen_id }} {
    display: grid;
    grid-template-columns: repeat({{ block.settings.products_per_row }}, 1fr);
    gap: 24px;
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  .ai-collection-carousel-track-{{ ai_gen_id }}.active {
    opacity: 1;
  }

  .ai-collection-carousel-track-{{ ai_gen_id }}.hidden {
    display: none;
  }

  .ai-collection-carousel-product-{{ ai_gen_id }} {
    background-color: {{ block.settings.card_background }};
    border-radius: {{ block.settings.card_border_radius }}px;
    overflow: hidden;
    transition: transform 0.3s ease;
  }

  .ai-collection-carousel-product-{{ ai_gen_id }}:hover {
    transform: translateY(-4px);
  }

  .ai-collection-carousel-product-image-{{ ai_gen_id }} {
    position: relative;
    width: 100%;
    padding-bottom: 125%;
    overflow: hidden;
    background-color: #f5f5f5;
  }

  .ai-collection-carousel-product-image-{{ ai_gen_id }} img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .ai-collection-carousel-product-image-placeholder-{{ ai_gen_id }} {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #f5f5f5;
  }

  .ai-collection-carousel-product-image-placeholder-{{ ai_gen_id }} svg {
    width: 60%;
    height: 60%;
    opacity: 0.3;
  }

  .ai-collection-carousel-product-info-{{ ai_gen_id }} {
    padding: 16px;
  }

  .ai-collection-carousel-product-title-{{ ai_gen_id }} {
    color: {{ block.settings.product_title_color }};
    font-size: {{ block.settings.product_title_size }}px;
    margin: 0 0 8px;
    overflow: hidden;
    text-overflow: ellipsis;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
  }

  .ai-collection-carousel-product-price-{{ ai_gen_id }} {
    color: {{ block.settings.product_price_color }};
    font-size: {{ block.settings.product_price_size }}px;
    font-weight: 600;
  }

  .ai-collection-carousel-empty-{{ ai_gen_id }} {
    text-align: center;
    padding: 60px 20px;
    color: {{ block.settings.title_color }};
    font-size: 16px;
  }

  @media screen and (max-width: 989px) {
    .ai-collection-carousel-container-{{ ai_gen_id }} {
      padding: 0 24px;
    }

    .ai-collection-carousel-header-{{ ai_gen_id }} {
      flex-direction: column;
      align-items: flex-start;
      margin-bottom: 32px;
    }

    .ai-collection-carousel-tabs-{{ ai_gen_id }} {
      justify-content: flex-start;
      width: 100%;
    }

    .ai-collection-carousel-track-{{ ai_gen_id }} {
      grid-template-columns: repeat(2, 1fr);
      gap: 16px;
    }

    .ai-collection-carousel-title-{{ ai_gen_id }} {
      font-size: {{ block.settings.title_size | times: 0.7 }}px;
    }
  }

  @media screen and (max-width: 749px) {
    .ai-collection-carousel-container-{{ ai_gen_id }} {
      padding: 0 16px;
    }

    .ai-collection-carousel-track-{{ ai_gen_id }} {
      grid-template-columns: 1fr;
    }

    .ai-collection-carousel-tabs-{{ ai_gen_id }} {
      gap: 16px;
    }

    .ai-collection-carousel-tab-{{ ai_gen_id }} {
      font-size: {{ block.settings.tab_size | times: 0.9 }}px;
    }
  }

{% comment %} new css added by me {% endcomment %}

  .ai-collection-carousel-content-abxvgbk1itnjjwdjhbaigenblock61a7919aahbd6{
    position: absolute;
    /* position: relative; */
    width: 100%;
    max-width: 55%;
    padding: 20px;
    box-sizing: border-box;
    top: 50%;
    right: 20px;
    transition: opacity 0.4s ease;
  }
{% endstyle %}

<collection-carousel-{{ ai_gen_id }}
  class="ai-collection-carousel-{{ ai_gen_id }}"
  style="{% if block.settings.background_image %}background-image: url('{{ block.settings.background_image | image_url: width: 2000 }}');{% else %}background-color: {{ block.settings.background_color }};{% endif %}"
  {{ block.shopify_attributes }}
>
  {% if block.settings.background_image and block.settings.overlay_opacity > 0 %}
    <div class="ai-collection-carousel-overlay-{{ ai_gen_id }}"></div>
  {% endif %}

  <div class="ai-collection-carousel-container-{{ ai_gen_id }}">
    <div class="ai-collection-carousel-header-{{ ai_gen_id }}">
      {% if block.settings.title != blank %}
        <h2 class="ai-collection-carousel-title-{{ ai_gen_id }}">{{ block.settings.title }}</h2>
      {% endif %}

      <div class="ai-collection-carousel-tabs-{{ ai_gen_id }}">
        {% for i in (1..5) %}
          {% liquid
            assign collection_key = 'collection_' | append: i
            assign collection = block.settings[collection_key]
          %}
          {% if collection != blank %}
            <button
              class="ai-collection-carousel-tab-{{ ai_gen_id }} {% if forloop.first %}active{% endif %}"
              data-collection-index="{{ forloop.index0 }}"
              aria-label="View {{ collection.title }}"
            >
              {{ collection.title }}
            </button>
          {% endif %}
        {% endfor %}
      </div>
    </div>

    <div class="ai-collection-carousel-content-{{ ai_gen_id }}">
      {% for i in (1..5) %}
        {% liquid
          assign collection_key = 'collection_' | append: i
          assign collection = block.settings[collection_key]
        %}
        {% if collection != blank %}
          <div
            class="ai-collection-carousel-track-{{ ai_gen_id }} {% if forloop.first %}active{% else %}hidden{% endif %}"
            data-collection-track="{{ forloop.index0 }}"
          >
            {% if collection.products.size > 0 %}
              {% for product in collection.products limit: block.settings.products_to_show %}
                <a
                  href="{{ product.url }}"
                  class="ai-collection-carousel-product-{{ ai_gen_id }}"
                >
                  <div class="ai-collection-carousel-product-image-{{ ai_gen_id }}">
                    {% if product.featured_image %}
                      <img
                        src="{{ product.featured_image | image_url: width: 600 }}"
                        alt="{{ product.featured_image.alt | escape }}"
                        loading="lazy"
                        width="600"
                        height="750"
                      >
                    {% else %}
                      <div class="ai-collection-carousel-product-image-placeholder-{{ ai_gen_id }}">
                        {{ 'product-1' | placeholder_svg_tag }}
                      </div>
                    {% endif %}
                  </div>
                  <div class="ai-collection-carousel-product-info-{{ ai_gen_id }}">
                    <h3 class="ai-collection-carousel-product-title-{{ ai_gen_id }}">
                      {{ product.title }}
                    </h3>
                    <div class="ai-collection-carousel-product-price-{{ ai_gen_id }}">
                      {{ product.price | money }}
                    </div>
                  </div>
                </a>
              {% endfor %}
            {% else %}
              <div class="ai-collection-carousel-empty-{{ ai_gen_id }}">
                No products found in this collection
              </div>
            {% endif %}
          </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</collection-carousel-{{ ai_gen_id }}>

<script>
  (function() {
    class CollectionCarousel{{ ai_gen_id }} extends HTMLElement {
      constructor() {
        super();
      }

      connectedCallback() {
        this.tabs = this.querySelectorAll('.ai-collection-carousel-tab-{{ ai_gen_id }}');
        this.tracks = this.querySelectorAll('.ai-collection-carousel-track-{{ ai_gen_id }}');
        this.setupEventListeners();
      }

      setupEventListeners() {
        this.tabs.forEach((tab) => {
          tab.addEventListener('click', () => {
            const collectionIndex = parseInt(tab.getAttribute('data-collection-index'));
            this.switchCollection(collectionIndex);
          });
        });
      }

      switchCollection(index) {
        this.tabs.forEach((tab, i) => {
          if (i === index) {
            tab.classList.add('active');
          } else {
            tab.classList.remove('active');
          }
        });

        this.tracks.forEach((track) => {
          const trackIndex = parseInt(track.getAttribute('data-collection-track'));
          if (trackIndex === index) {
            track.classList.remove('hidden');
            setTimeout(() => {
              track.classList.add('active');
            }, 10);
          } else {
            track.classList.remove('active');
            setTimeout(() => {
              track.classList.add('hidden');
            }, 400);
          }
        });
      }
    }

    customElements.define('collection-carousel-{{ ai_gen_id }}', CollectionCarousel{{ ai_gen_id }});
  })();
</script>

{% schema %}
{
  "name": "Collection carousel",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "Background"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background image"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#f5f5f5"
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Overlay color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 80,
      "step": 5,
      "unit": "%",
      "label": "Overlay opacity",
      "default": 30
    },
    {
      "type": "range",
      "id": "section_padding_top",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Top padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "section_padding_bottom",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Bottom padding",
      "default": 60
    },
    {
      "type": "header",
      "content": "Title"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Shop by collection"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "title_size",
      "min": 16,
      "max": 72,
      "step": 2,
      "unit": "px",
      "label": "Title size",
      "default": 48
    },
    {
      "type": "header",
      "content": "Collection tabs"
    },
    {
      "type": "collection",
      "id": "collection_1",
      "label": "Collection 1"
    },
    {
      "type": "collection",
      "id": "collection_2",
      "label": "Collection 2"
    },
    {
      "type": "collection",
      "id": "collection_3",
      "label": "Collection 3"
    },
    {
      "type": "collection",
      "id": "collection_4",
      "label": "Collection 4"
    },
    {
      "type": "collection",
      "id": "collection_5",
      "label": "Collection 5"
    },
    {
      "type": "color",
      "id": "tab_color",
      "label": "Tab color",
      "default": "#666666"
    },
    {
      "type": "color",
      "id": "tab_active_color",
      "label": "Active tab color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "tab_size",
      "min": 10,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Tab size",
      "default": 14
    },
    {
      "type": "header",
      "content": "Product carousel"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 2,
      "max": 12,
      "step": 1,
      "label": "Products to show",
      "default": 4
    },
    {
      "type": "range",
      "id": "products_per_row",
      "min": 2,
      "max": 5,
      "step": 1,
      "label": "Products per row",
      "default": 4
    },
    {
      "type": "color",
      "id": "card_background",
      "label": "Card background",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "card_border_radius",
      "min": 0,
      "max": 20,
      "step": 2,
      "unit": "px",
      "label": "Card border radius",
      "default": 0
    },
    {
      "type": "color",
      "id": "product_title_color",
      "label": "Product title color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "product_title_size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Product title size",
      "default": 14
    },
    {
      "type": "color",
      "id": "product_price_color",
      "label": "Product price color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "product_price_size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Product price size",
      "default": 14
    }
  ],
  "presets": [
    {
      "name": "Collection carousel"
    }
  ]
}
{% endschema %}
